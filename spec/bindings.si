# Result should be 1+2+3+4+5 = 15
nested_bindings () :=
    val a := 1 + 2 in
    val a := a + 3 in
    val a := a + 4 in
    val a := a + 5 in
    a
;

# Result should be 1+2 = 3
mixed_lookup () :=
    val a := 1 in
    val b := 2 in
    val c := a + b in
    c
;
